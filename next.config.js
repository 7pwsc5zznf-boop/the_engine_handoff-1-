/** @type {import('next').NextConfig} */
const nextConfig = {
  // Vercel-optimized defaults. No custom server, no standalone output needed —
  // Vercel auto-detects Next.js and applies optimal build/serverless settings.
  reactStrictMode: true,
  poweredByHeader: false,
};

module.exports = nextConfig;
